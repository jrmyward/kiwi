<div class="container">
  <div class="row">
    <div class="col-sm-8">
      <% @events.group_by { |event| event.local_date }.each do |date, events| %>
        <%= render partial: 'day_summary', locals: { events: events, date: date } %>
      <% end %>
    </div>
  </div>
</div>
