<h5 class="filter-title">Your country<sup data-toggle="tooltip" data-placement="top" title="Some events are only relevant within a certain country such as a national holiday or a TV show time. Everyone can see events with the Global label.">?</sup> is set to:</h5>
<select class="form-control" name="country">
  <% @all_countries.each do |country| %>
    <option value="<%=country.code%>" <%= (country.code == @country) ? 'selected' : '' %>><%=country.en_name%></option>
  <% end %>
</select>
<div class="clearfix"></div>
