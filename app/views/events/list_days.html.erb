<% @events.group_by { |event| event.get_local_datetime(@time_zone) }.each do |date, events| %>
  <%= render partial: 'day_summary', locals: { events: events, date: date, repository: @repository, subkasts: @subkasts, country: @country } %>
<% end %>
